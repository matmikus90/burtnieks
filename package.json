{
  "name": "burtnieks",
  "version": "1.7.0",
  "private": true,
  "main": "server.js",
  "scripts": {
    "start": "node server.js",
    "dev": "nodemon server.js",
    "check": "node scripts/check.js && node scripts/check-achievements.js && node scripts/check-board-grid.js && node scripts/check-word-vote.js && node scripts/check-final-scoring.js && node scripts/check-layout-connection.js && node scripts/check-touch-chat.js"
  },
  "dependencies": {
    "express": "^5.2.1",
    "socket.io": "^4.8.3"
  },
  "devDependencies": {
    "nodemon": "^3.1.10"
  }
}
